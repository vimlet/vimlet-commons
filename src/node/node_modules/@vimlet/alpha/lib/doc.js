var path = require("path");
var commons = require("@vimlet/commons");

var cwd = process.cwd();

exports.doc = function(packagePath, params, cb) {
    commons.run.exec(path.join(cwd, "node_modules/.bin/vimlet-doc"), params, packagePath, null, cb);
};

